String.prototype.toJadenCase = function (jadenStr) {
  let jadenArray = jadenStr.split('');
  jadenArray.map(word =>  {
    return word[0].toUpperCase() +
    word.slice(1)}
                ).join('');}
